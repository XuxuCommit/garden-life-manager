import { Rectangle } from "./Rectangle";

export function intersects(
  a: Rectangle,
  b: Rectangle
) {
  return !(
    a.x + a.width < b.x ||
    b.x + b.width < a.x ||
    a.y + a.height < b.y ||
    b.y + b.height < a.y
  );
}